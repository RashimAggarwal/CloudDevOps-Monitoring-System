global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "cloudshield-backend"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "cloudshield-backend:5000"
